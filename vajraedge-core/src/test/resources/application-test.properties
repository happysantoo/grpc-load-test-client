# Test-specific properties to avoid port conflicts

# Disable WebSocket server in tests (prevents SockJS/Netty from binding to ports)
vajraedge.websocket.enabled=false

# Disable gRPC server in tests (already conditional but set explicitly)
vajraedge.grpc.enabled=false
