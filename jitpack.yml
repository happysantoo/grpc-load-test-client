# JitPack configuration for VajraEdge
# Ensures Java 21 is used for building

jdk:
  - openjdk21

before_install:
  - sdk install java 21.0.1-open || true
  - sdk use java 21.0.1-open

install:
  - ./gradlew clean :vajraedge-sdk:publishToMavenLocal -x test
